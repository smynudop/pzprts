import * as Lib from "@udop/penpa-player-lib";
import { createPlayer } from "./createPlayer";

export const IchimagaPlayer = createPlayer(Lib.Ichimaga)
export const IchimagamPlayer = createPlayer(Lib.Ichimagam)
export const IchimagaxPlayer = createPlayer(Lib.Ichimagax)